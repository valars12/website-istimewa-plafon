<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Jika file atau folder tidak ada, arahkan ke index.html (SPA)
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

